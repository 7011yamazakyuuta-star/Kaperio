/* Local-only UI. All user-provided names are inserted as text. */
const $ = id => document.getElementById(id);
const state = {jobs: [], selected: null, page: 0, tab: 'unlock', mode: 'known', preview: '', polling: false};
const busy = new Set(['queued', 'recovering', 'pausing', 'converting', 'unlocking']);
const labels = {locked:'未解除',ready:'開封済み',queued:'探索待ち',recovering:'探索中',pausing:'停止中',paused:'一時停止',converting:'書き出し中',exhausted:'候補の探索完了',error:'エラー',cancelled:'中止',unlocking:'照合中'};
const kinds = {pdf:'PDF',unlocked:'解除済みファイル',image_pdf:'画像PDF',images:'ページ画像 ZIP',word:'Word・ページ画像',text:'抽出テキスト'};
function icons(){lucide.createIcons({attrs:{'stroke-width':1.7}})}
function el(tag, cls, text){const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=text;return n}
function icon(name){const n=el('i');n.dataset.lucide=name;return n}
function toast(message){$('toast').textContent=message;$('toast').hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>{$('toast').hidden=true},6000)}
function bytes(n){return n>1048576?(n/1048576).toFixed(1)+' MB':Math.round(n/1024)+' KB'}
async function api(path,data){const options=data===undefined?{}:{method:'POST',headers:{'Content-Type':'application/json','X-Kaperio':'1'},body:JSON.stringify(data)};const r=await fetch(path,options);const result=await r.json();if(!r.ok)throw Error(result.error||'処理に失敗しました');return result}
function job(){return state.jobs.find(j=>j.id===state.selected)}
function setTab(tab){state.tab=tab;renderDetail()}
function selectJob(id){if(state.selected!==id){state.selected=id;state.page=0;state.preview='';$('password').value='';$('password-result').hidden=true;state.tab='unlock'}render()}
function renderList(){const list=$('file-list');list.replaceChildren();const query=$('search').value.toLowerCase();const filter=$('filter').value;const jobs=state.jobs.filter(j=>j.name.toLowerCase().includes(query)&&(filter==='all'||filter==='ready'&&j.available||filter==='locked'&&!j.available||filter==='active'&&busy.has(j.state)));$('file-count').textContent=jobs.length;for(const j of jobs){const b=el('button','file-item'+(j.id===state.selected?' selected':''));b.setAttribute('aria-pressed',String(j.id===state.selected));b.append(icon(j.available?'file-check-2':'file-lock-2'));const wrap=el('div');wrap.append(el('span','file-name',j.name));const detail=el('div','file-detail');detail.append(el('span','tiny-status',labels[j.state]),el('span','',bytes(j.size)));wrap.append(detail);b.append(wrap);b.onclick=()=>selectJob(j.id);list.append(b)}if(!jobs.length)list.append(el('p','empty-list','ファイルはありません'))}
function renderDetail(){const j=job();$('empty').hidden=!!j;$('document').hidden=!j;if(!j)return;$('document-name').textContent=j.name;$('document-size').textContent=bytes(j.size);$('document-pages').textContent=j.info.pages?j.info.pages+' ページ':'';$('document-encryption').textContent=j.info.encryption;$('document-badge').textContent=labels[j.state];$('document-badge').className='badge'+(j.available?' ready':'');$('tab-unlock').setAttribute('aria-selected',state.tab==='unlock');$('tab-export').setAttribute('aria-selected',state.tab==='export');$('unlock-panel').hidden=state.tab!=='unlock';$('export-panel').hidden=state.tab!=='export';$('tab-export').disabled=!j.available;$('ready-state').hidden=!j.available;$('locked-state').hidden=j.available;$('known-form').hidden=state.mode!=='known';$('recovery-form').hidden=state.mode!=='recover';$('known-mode').classList.toggle('selected',state.mode==='known');$('recover-mode').classList.toggle('selected',state.mode==='recover');$('unlocked-download').href=`/api/jobs/${j.id}/download/unlocked${j.info.extension}`;$('unlocked-download').lastChild.textContent='解除済み'+j.info.extension.toUpperCase();$('reveal-password').hidden=!j.has_password;for(const form of ['known-form','recovery-form','export-form'])for(const node of $(form).querySelectorAll('input,select,textarea,button'))node.disabled=busy.has(j.state)||form==='export-form'&&!j.available;$('recovery-form').querySelector('button[type=submit]').disabled=busy.has(j.state)||!j.info.recoverable;
const outputs=$('outputs');outputs.replaceChildren();for(const out of j.outputs){const row=el('div','output-row');row.append(icon(out.kind==='word'?'file-type':'file-check'));const detail=el('div');detail.append(el('strong','',kinds[out.kind]),el('small','',bytes(out.bytes)));const link=el('a','icon');link.href=`/api/jobs/${j.id}/download/${encodeURIComponent(out.file)}`;link.title=kinds[out.kind]+'をダウンロード';link.setAttribute('aria-label',link.title);link.append(icon('download'));row.append(detail,link);outputs.append(row)}
$('export-form').hidden=!j.can_convert;$('job-status-label').textContent=labels[j.state];$('job-message').textContent=j.message;$('pause-job').hidden=!['recovering','queued'].includes(j.state);$('cancel-job').hidden=!['queued','recovering','converting'].includes(j.state);$('remove-file').disabled=busy.has(j.state);$('resume-job').hidden=!['paused','error','cancelled'].includes(j.state)||!j.can_resume;const m=j.metrics||{};const percent=j.state==='converting'?(j.export_progress||0):m.total?Math.min(100,m.tested/m.total*100):0;$('job-progress').value=percent;$('speed').textContent=m.speed?(m.speed>=1e6?(m.speed/1e6).toFixed(1)+' MH/s':Math.round(m.speed).toLocaleString()+' H/s'):'—';$('gpu-temp').textContent=m.temperature?m.temperature+' °C':'—';$('percentage').textContent=m.total?percent.toFixed(1)+'%':'—';const warnings=$('warnings');warnings.replaceChildren();for(const warning of [...j.warnings,...(j.info.recovery_note?[j.info.recovery_note]:[])])warnings.append(el('p','',warning));$('page-indicator').textContent=j.has_pdf?`${state.page+1} / ${j.info.pages}`:'—';$('previous-page').disabled=!j.has_pdf||state.page<=0;$('next-page').disabled=!j.has_pdf||state.page>=j.info.pages-1;$('locked-preview').hidden=j.available;$('page-image').hidden=!j.has_pdf;$('contents-preview').hidden=!j.available||j.has_pdf;const entries=$('contents-preview');entries.replaceChildren();for(const item of j.contents||[]){const row=el('div','contents-row');row.append(icon(j.info.format==='zip'?'file':'file-text'),el('span','',item.name));if(item.size!==null)row.append(el('small','',bytes(item.size)));entries.append(row)}if(j.has_pdf){const key=`${j.id}/${state.page}`;if(key!==state.preview){state.preview=key;$('page-image').src=`/api/jobs/${j.id}/preview?page=${state.page}`}}icons()}
function render(){renderList();renderDetail();icons()}
async function refresh(){if(state.polling)return;state.polling=true;try{const result=await api('/api/jobs');state.jobs=result.jobs;if(!state.jobs.some(j=>j.id===state.selected))state.selected=null;if(!state.selected&&state.jobs.length)state.selected=state.jobs[0].id;render()}catch(e){toast(e.message)}finally{state.polling=false}}
async function action(name,data={}){const id=state.selected;try{await api(`/api/jobs/${id}/${name}`,data);$('toast').hidden=true;await refresh()}catch(e){toast(e.message)}}
function estimate(){try{let count;if($('strategy').value==='dictionary'){count=BigInt(new Set($('words').value.split(/\r?\n/).filter(Boolean)).size)}else{const sizes={lower:26,upper:26,digits:10,symbols:33};const size=[...document.querySelectorAll('[name=charset]:checked')].reduce((s,n)=>s+sizes[n.value],0);const fixed=$('prefix').value.length+$('suffix').value.length;const low=Number($('min-length').value),high=Number($('max-length').value);if(low<fixed||high<low||high>16||!size)throw Error();count=0n;for(let n=low;n<=high;n++)count+=BigInt(size)**BigInt(n-fixed)}$('candidate-count').textContent=count.toLocaleString()+' 通り'}catch{$('candidate-count').textContent='条件を確認してください'}}
async function importFiles(files){for(const file of files){try{if(file.size>104857600)throw Error('100MBを超えるファイルです。');toast(file.name+' を読み込み中');const r=await fetch('/api/import',{method:'POST',headers:{'X-Kaperio':'1','X-Filename':encodeURIComponent(file.name),'Content-Type':'application/pdf'},body:file});const data=await r.json();if(!r.ok)throw Error(data.error);state.selected=data.id;state.tab='unlock';state.page=0;state.preview='';$('password-result').hidden=true;await refresh()}catch(e){toast(e.message)}}$('files').value='';/* Per-file errors remain visible. */}
for(const id of ['add-files','import-bottom','empty-add'])$(id).onclick=()=>$('files').click();$('files').onchange=()=>importFiles($('files').files);$('search').oninput=render;$('filter').onchange=render;$('tab-unlock').onclick=()=>setTab('unlock');$('tab-export').onclick=()=>setTab('export');$('known-mode').onclick=()=>{state.mode='known';renderDetail()};$('recover-mode').onclick=()=>{state.mode='recover';renderDetail()};$('toggle-password').onclick=()=>{$('password').type=$('password').type==='password'?'text':'password'};$('known-form').onsubmit=async e=>{e.preventDefault();await action('unlock',{password:$('password').value});if(job()?.available)$('password').value=''};$('recovery-form').onsubmit=e=>{e.preventDefault();action('recover',{strategy:$('strategy').value,min:Number($('min-length').value),max:Number($('max-length').value),prefix:$('prefix').value,suffix:$('suffix').value,charsets:[...document.querySelectorAll('[name=charset]:checked')].map(n=>n.value),words:$('words').value,minutes:Number($('minutes').value),temperature:Number($('temperature').value),workload:Number($('workload').value),kernel:$('kernel').value,devices:$('devices').value})};$('strategy').onchange=()=>{$('mask-fields').hidden=$('strategy').value!=='mask';$('dictionary-fields').hidden=$('strategy').value!=='dictionary';estimate()};$('recovery-form').oninput=estimate;$('load-words').onclick=()=>$('word-file').click();$('word-file').onchange=async()=>{const f=$('word-file').files[0];if(f){if(f.size>12*1024*1024)return toast('候補ファイルが大きすぎます。');$('words').value=await f.text();estimate()}};for(const [id,name] of [['pause-job','pause'],['resume-job','resume'],['cancel-job','cancel']])$(id).onclick=()=>action(name);$('previous-page').onclick=()=>{state.page--;renderDetail()};$('next-page').onclick=()=>{state.page++;renderDetail()};$('page-image').onerror=()=>toast('プレビューを生成できませんでした。');$('reveal-password').onclick=async()=>{try{const r=await api(`/api/jobs/${state.selected}/password`,{});$('password-result').textContent=r.password;$('password-result').hidden=!$('password-result').hidden}catch(e){toast(e.message)}};
const notes={pdf:'Officeの印刷レイアウト',image_pdf:'画像のみ・テキスト検索なし',word:'ページ画像・本文の文字編集なし',images:'PNG / 1ページ1枚',text:'OCRなし・元のテキスト層に依存（文字化けの可能性あり）'};function exportNote(){$('format-note').textContent=notes[$('export-kind').value];$('image-options').hidden=$('export-kind').value==='text'}$('export-kind').onchange=exportNote;$('export-form').onsubmit=e=>{e.preventDefault();action('export',{kind:$('export-kind').value,dpi:Number($('dpi').value),grayscale:$('color').value==='gray'})};$('settings-open').onclick=async()=>{try{const s=await api('/api/settings');$('hashcat-path').value=s.hashcat;$('zip2john-path').value=s.zip2john||'';$('output-path').textContent=s.output_dir;$('settings-dialog').showModal()}catch(e){toast(e.message)}};$('settings-close').onclick=()=>$('settings-dialog').close();$('settings-form').onsubmit=async e=>{e.preventDefault();try{await api('/api/settings',{hashcat:$('hashcat-path').value,zip2john:$('zip2john-path').value});toast('設定を保存しました');await refresh()}catch(e){toast(e.message)}};$('diagnostics').onclick=async()=>{$('diagnostics').disabled=true;$('diagnostics-result').hidden=false;$('diagnostics-result').textContent='GPUを確認しています…';try{const r=await api('/api/diagnostics',{});$('diagnostics-result').textContent=r.text}catch(e){$('diagnostics-result').textContent=e.message}finally{$('diagnostics').disabled=false}};$('shutdown').onclick=async()=>{try{await api('/api/shutdown',{});clearInterval(poll);toast('アプリを終了しました。');document.querySelectorAll('button').forEach(b=>b.disabled=true)}catch(e){toast(e.message)}};let drag=0;window.addEventListener('dragenter',e=>{if(e.dataTransfer.types.includes('Files')){e.preventDefault();drag++;$('drop-overlay').hidden=false}});window.addEventListener('dragleave',()=>{if(--drag<=0){drag=0;$('drop-overlay').hidden=true}});window.addEventListener('dragover',e=>e.preventDefault());window.addEventListener('drop',e=>{e.preventDefault();drag=0;$('drop-overlay').hidden=true;importFiles(e.dataTransfer.files)});icons();estimate();exportNote();refresh();const poll=setInterval(refresh,1800);
$('remove-file').onclick=()=>{const j=job();if(j){$('remove-name').textContent=j.name;$('remove-dialog').showModal()}};
$('remove-cancel').onclick=()=>$('remove-dialog').close();
$('remove-confirm').onclick=async()=>{await action('remove');$('remove-dialog').close()};
if('serviceWorker' in navigator)navigator.serviceWorker.register('/service-worker.js').catch(()=>{});
